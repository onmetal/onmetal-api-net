---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: natgatewayautoscalers.apinet.api.onmetal.de
spec:
  group: apinet.api.onmetal.de
  names:
    kind: NATGatewayAutoscaler
    listKind: NATGatewayAutoscalerList
    plural: natgatewayautoscalers
    singular: natgatewayautoscaler
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NATGatewayAutoscaler is the schema for the natgatewayautoscalers
          API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              maxPublicIPs:
                description: MaxPublicIPs is the maximum number of public IPs to allocate
                  for a NAT Gateway.
                format: int32
                type: integer
              minPublicIPs:
                description: MinPublicIPs is the minimum number of public IPs to allocate
                  for a NAT Gateway.
                format: int32
                type: integer
              natGatewayRef:
                description: NATGatewayRef points to the target NATGateway to scale.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - natGatewayRef
            type: object
          status:
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
