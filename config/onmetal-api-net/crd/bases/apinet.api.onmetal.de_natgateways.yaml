---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: natgateways.apinet.api.onmetal.de
spec:
  group: apinet.api.onmetal.de
  names:
    kind: NATGateway
    listKind: NATGatewayList
    plural: natgateways
    singular: natgateway
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NATGateway is the schema for the natgateways API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              ipFamily:
                description: IPFamily is the IP family of the NAT gateway.
                type: string
              networkRef:
                description: NetworkRef references the network the NAT gateway is
                  part of.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              portsPerNetworkInterface:
                default: 2048
                description: PortsPerNetworkInterface specifies how many ports to
                  allocate per network interface.
                format: int32
                maximum: 64512
                minimum: 1
                type: integer
              publicIPRefs:
                description: PublicIPRefs is the list of public IPs to use for the
                  NAT Gateway.
                items:
                  description: LocalObjectReference contains enough information to
                    let you locate the referenced object inside the same namespace.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
            required:
            - ipFamily
            - networkRef
            - portsPerNetworkInterface
            type: object
          status:
            properties:
              ips:
                description: IPs are the IPs currently in-use by the NAT gateway.
                items:
                  type: string
                type: array
              usedNATIPs:
                description: UsedNATIPs is the number of NAT IPs in-use.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
