---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: loadbalancers.apinet.api.onmetal.de
spec:
  group: apinet.api.onmetal.de
  names:
    kind: LoadBalancer
    listKind: LoadBalancerList
    plural: loadbalancers
    singular: loadbalancer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadBalancer is the schema for the loadbalancers API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              ipSelector:
                description: IPSelector selects the IPs to allocate for the load balancer.
                  If empty or not present, this load balancer is assumed to have an
                  external process claiming public IPs, which onmetal-api-net will
                  not modify.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: A label selector requirement is a selector that
                        contains values, a key, and an operator that relates the key
                        and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: operator represents a key's relationship to
                            a set of values. Valid operators are In, NotIn, Exists
                            and DoesNotExist.
                          type: string
                        values:
                          description: values is an array of string values. If the
                            operator is In or NotIn, the values array must be non-empty.
                            If the operator is Exists or DoesNotExist, the values
                            array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: matchLabels is a map of {key,value} pairs. A single
                      {key,value} in the matchLabels map is equivalent to an element
                      of matchExpressions, whose key field is "key", the operator
                      is "In", and the values array contains only "value". The requirements
                      are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              ips:
                description: IPs are the internal IPs of the load balancer. Can only
                  be specified when Type is LoadBalancerTypeInternal.
                items:
                  type: string
                type: array
              networkInterfaceSelector:
                description: NetworkInterfaceSelector selects the network interfaces
                  to target with this load balancer. If empty or not present, this
                  load balancer is assumed to have an external process managing its
                  routing, which onmetal-api-net will not modify.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: A label selector requirement is a selector that
                        contains values, a key, and an operator that relates the key
                        and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: operator represents a key's relationship to
                            a set of values. Valid operators are In, NotIn, Exists
                            and DoesNotExist.
                          type: string
                        values:
                          description: values is an array of string values. If the
                            operator is In or NotIn, the values array must be non-empty.
                            If the operator is Exists or DoesNotExist, the values
                            array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: matchLabels is a map of {key,value} pairs. A single
                      {key,value} in the matchLabels map is equivalent to an element
                      of matchExpressions, whose key field is "key", the operator
                      is "In", and the values array contains only "value". The requirements
                      are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              networkRef:
                description: NetworkRef references the network the load balancer is
                  part of.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              ports:
                description: Ports are the ports the load balancer should allow. If
                  empty, the load balancer allows all ports.
                items:
                  properties:
                    endPort:
                      description: EndPort marks the end of the port range to allow.
                        If unspecified, only a single port, Port, will be allowed.
                      format: int32
                      type: integer
                    port:
                      description: Port is the port to allow.
                      format: int32
                      type: integer
                    protocol:
                      default: TCP
                      description: Protocol is the protocol the load balancer should
                        allow. If not specified, defaults to TCP.
                      type: string
                  required:
                  - port
                  type: object
                type: array
              replicasPerPartition:
                default: 1
                description: ReplicasPerPartition specifies the number of replicas
                  for a load balancer. Pointer to distinguish between zero and explicit
                  zero.
                format: int32
                type: integer
              type:
                description: Type specifies the type of load balancer.
                type: string
            required:
            - networkRef
            - type
            type: object
          status:
            properties:
              ips:
                description: IPs are the IPs used currently by the load balancer.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
