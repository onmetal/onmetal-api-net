---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: natgatewayroutings.apinet.api.onmetal.de
spec:
  group: apinet.api.onmetal.de
  names:
    kind: NATGatewayRouting
    listKind: NATGatewayRoutingList
    plural: natgatewayroutings
    singular: natgatewayrouting
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NATGatewayRouting is the schema for the natgatewayroutings API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          destinations:
            items:
              properties:
                name:
                  type: string
                natIP:
                  description: NATIP is a NATed IP.
                  properties:
                    endPort:
                      description: EndPort is the last port to use.
                      format: int32
                      type: integer
                    ip:
                      description: IP is the NATed IP to use.
                      type: string
                    port:
                      description: Port is the first port to use.
                      format: int32
                      type: integer
                  required:
                  - endPort
                  - ip
                  - port
                  type: object
                nodeRef:
                  description: NodeRef references the node the destination network
                    interface is on.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                uid:
                  description: UID is a type that holds unique ID values, including
                    UUIDs.  Because we don't ONLY use UUIDs, this is an alias to string.  Being
                    a type captures intent and helps make sure that UIDs and names
                    do not get conflated.
                  type: string
              required:
              - name
              - natIP
              - nodeRef
              - uid
              type: object
            type: array
          ipFamily:
            description: IPFamily represents the IP Family (IPv4 or IPv6). This type
              is used to express the family of an IP expressed by a type (e.g. service.spec.ipFamilies).
            type: string
          ips:
            description: IPs are the IPs the routing is for.
            items:
              type: string
            type: array
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
        required:
        - ipFamily
        type: object
    served: true
    storage: true
