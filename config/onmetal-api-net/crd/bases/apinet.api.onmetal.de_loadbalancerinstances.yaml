---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: loadbalancerinstances.apinet.api.onmetal.de
spec:
  group: apinet.api.onmetal.de
  names:
    kind: LoadBalancerInstance
    listKind: LoadBalancerInstanceList
    plural: loadbalancerinstances
    singular: loadbalancerinstance
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadBalancerInstance is the schema for the loadbalancerinstances
          API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              ips:
                description: IP are the IPs of the load balancer instance.
                items:
                  type: string
                type: array
              networkRef:
                description: NetworkRef references the network the load balancer instance
                  is part of.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              nodeRef:
                description: NodeRef references the node hosting the load balancer
                  instance. If empty, a scheduler of the partition sets this.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              partitionRef:
                description: PartitionRef references the partition hosting the load
                  balancer instance.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              ports:
                description: Ports are the ports the load balancer instance should
                  allow. If empty, the load balancer instance allows all ports.
                items:
                  properties:
                    endPort:
                      description: EndPort marks the end of the port range to allow.
                        If unspecified, only a single port, Port, will be allowed.
                      format: int32
                      type: integer
                    port:
                      description: Port is the port to allow.
                      format: int32
                      type: integer
                    protocol:
                      default: TCP
                      description: Protocol is the protocol the load balancer should
                        allow. If not specified, defaults to TCP.
                      type: string
                  required:
                  - port
                  type: object
                type: array
              type:
                description: Type specifies the type of load balancer instance.
                type: string
            required:
            - networkRef
            - partitionRef
            - type
            type: object
          status:
            properties:
              collisionCount:
                format: int32
                type: integer
              ips:
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
